# Week 4 Day 3：真实用例与自动化测试

## 当天目标

用真实 C/C++ 和主要失败场景验证完整链路，把当天发现的协议边界变成可重复回归测试。

## 开始条件

- 至少一个 C 示例端到端成功；
- 工作流状态和错误入口已完成；
- 测试环境可控制 LLVM 服务。

## 上午任务：真实成功场景

1. [x] 测试多个 C 示例；
2. [x] 测试至少一个 C++ 示例；
3. [x] 测试多函数程序；
4. [x] 测试产生变化和未变化 Pass 的程序；
5. [x] 检查特殊函数名、长 IR 和 Pass 顺序；
6. [x] 将新的边界 Payload 脱敏后保存为 Fixture。

### 上午执行记录

- 使用本地 LLVM 14 服务运行 `npm run test:day3-real-cases`，5 个真实成功
  场景全部通过；
- 3 组不同 C 输入加 Day 2 多函数 C 输入均通过，分别覆盖基础调用、循环、
  分支、多函数、特殊符号和长签名；
- C++ 的命名空间、重载运算符和函数模板输入通过 `clang++` 编译并产生 3 个
  函数、205 个 Pass，其中 8 个改变 IR；
- 全部场景同时包含变化和未变化 Pass，并验证 Pass 总数、ID 唯一、连续顺序、
  `changed` 与前后 IR 一致，以及 Schema 和 Adapter 可完整消费响应；
- 特殊函数名 `special.function-$case` 在 Schema/Adapter 后保持原样；对应响应为
  411,159 字符且未截断；
- 新的脱敏 Fixture 保存为
  `src/test-data/compiler-optimisation/day3-special-name-backend.json`，不包含
  `/tmp/` 路径或后端 UUID。

## 下午任务：失败和自动化

1. [x] 测试空文件和错误扩展名；
2. [x] 测试 C/C++ 语法错误；
3. [x] 测试 LLVM 服务不可用；
4. [x] 测试优化超时；
5. [x] 测试无效 JSON 和不支持版本；
6. [x] 添加关键端到端或集成测试；
7. [x] 为每个真实 Schema 差异增加回归测试；
8. [x] 验证连续运行不会泄露旧状态。

### 下午执行记录

- `npm run test:day3-failure-cases` 在真实 LLVM 14 服务上验证空输入、错误
  扩展名、C/C++ 语法错误、结构化端点错误扩展名、畸形 JSON 和不支持的
  Schema 版本；
- 修复畸形 JSON 被当作 500 的协议问题；LLVM 服务现在返回安全的 HTTP 400
  `{ "error": "invalid JSON body" }`；
- 新增 LLVM HTTP 客户端集成测试，使用受控本地服务验证成功响应、无效 JSON、
  编译/优化 HTTP 错误、连接拒绝和结构化优化超时的 tRPC 错误映射；
- 应用内浏览器验证空输入、错误扩展名、C++ 编译错误和 LLVM 服务停止场景；
  错误后输入保留、提交按钮恢复，远程运行开始时旧 Workspace 被清除；
- 连续运行 `first.c` 和 `second.c`：第一次主动选择 `first_only` 并启用
  Changed 过滤；第二次只显示 `second.c`/`second_only`，过滤恢复 All，函数与
  Pass 选择重新初始化；
- 真实 Schema 差异已有回归覆盖：旧原始响应、缺失指标、特殊函数名、完整长 IR、
  Pass 总数/ID/顺序/`changed` 一致性和不支持版本；未发现新的 P0 数据一致性问题。

## 一致性检查

- Pass 总数；
- ID 唯一；
- 顺序稳定；
- `changed` 与 IR 一致；
- 指标缺失语义；
- CFG 引用有效；
- C++ 字符安全显示；
- 响应未截断。

## 完成清单

- [x] C 和 C++ 成功路径通过；
- [x] 多函数通过；
- [x] 主要失败路径通过；
- [x] 回归 Fixture 已增加；
- [x] 关键自动化测试通过；
- [x] 连续运行状态正确；
- [x] P0 数据一致性问题已修复（未发现未解决的 P0 问题）。

## 风险与处理

- 测试不稳定：固定小型测试程序和明确超时；
- 大响应截断：检查 Express Limit、Buffer 和 tRPC 传输；
- C++ 结果异常：确认文件名扩展和 `clang++` 路径。

## 下一日交接

提供稳定测试集合、性能用大型 Fixture、已知失败和部署候选提交。
